<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
<% include ./partials/head.ejs %>
  </head>
  <body>

    <% if(currentUser) { %>
      <h1> Welcome <%=currentUser.username%>! </h1>
    <% } else { %>
      <div class="contianer">
        <div class="firstScreen">


      <img class = 'logo' src="/images/logo.png" alt="">

      <ul class="nav justify-content-center">
        <li class="nav-item">
          <a class = "btn btn-primary loginButton" href="/sessions/new">LOG IN</a>
        </li>
        <li class="nav-item">
           <a class = "btn btn-success signUPButton" href="users/new">SIGN UP</a>
        </li>
      </div>
    </div>
    <% } %>

    <% if(currentUser) { %>
    <main>
           <a href="/pghfree">Continue to PghFree</a>
    </main>
  <% } %>

    <% if(currentUser) { %>

        <form action="/sessions?_method=DELETE" method="POST">
            <input type="submit" value="Logout"/>
        </form>
<% } %>
  </body>
</html>
